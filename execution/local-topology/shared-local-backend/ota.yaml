#
#                █████
#               ░░███
#       ██████  ███████    ██████
#      ███░░███░░░███░    ░░░░░███
#     ░███ ░███  ░███      ███████
#     ░███ ░███  ░███ ███ ███░░███
#     ░░██████   ░░█████ ░░████████
#      ░░░░░░     ░░░░░   ░░░░░░░░
#
#   Copyright (C) 2026 — 2026, Ota. All Rights Reserved.
#
#   DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
#
#   Licensed under the Apache License, Version 2.0. See LICENSE for the full license text.
#   You may not use this file except in compliance with that License.
#   Unless required by applicable law or agreed to in writing, software distributed under the
#   License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND,
#   either express or implied. See the License for the specific language governing permissions
#   and limitations under the License.
#
#   If you need additional information or have any questions, please email: os@ota.run

version: 1
project:
  name: shared-local-backend
  description: Advanced shared local backend example with two long-running service entrypoints that intentionally reuse one container boundary
execution:
  default_context: app
  contexts:
    app:
      backend: container
      lifecycle: persistent
      container:
        image: node:24-bookworm
      requirements:
        runtimes:
          node: ">=24"
        tools:
          pnpm: "*"
  local_backends:
    workbench:
      backend: container
      lifecycle: persistent
      context: app
tasks:
  dev:
    description: Start the API service inside the shared workbench backend
    run: pnpm dev:api
    runtime:
      kind: service
      backend_binding: workbench
      listeners:
        http:
          protocol: http
          bind:
            address: 0.0.0.0
            port:
              mode: fixed
              value: 8080
          project:
            host:
              address: 127.0.0.1
              primary: true
              port:
                mode: fixed
                value: 8080
              path: /
    notes: |
      Example: `ota run dev`
      This task is one member of the shared local backend family.
  dev:debug:
    description: Start the same API shape with a debug-oriented operator profile
    run: LOG_LEVEL=debug pnpm dev:api
    runtime:
      kind: service
      backend_binding: workbench
      listeners:
        http:
          protocol: http
          bind:
            address: 0.0.0.0
            port:
              mode: fixed
              value: 8080
          project:
            host:
              address: 127.0.0.1
              primary: true
              port:
                mode: fixed
                value: 8080
              path: /
    notes: |
      Example: `ota run dev:debug`
      This task intentionally resolves the same container image, publications, and memory shape as `dev` so ota can reuse the declared shared backend boundary.
agent:
  entrypoint: dev
  default_task: dev:debug
  safe_tasks:
    - dev
    - dev:debug
  verify_after_changes:
    - dev:debug
  writable_paths:
    - src
    - docs
  protected_paths:
    - ota.yaml
  bootstrap:
    ota:
      note: Only install ota if it is missing and installation is approved.
      sh: curl -fsSL https://dist.ota.run/install.sh | sh
      powershell: irm https://dist.ota.run/install.ps1 | iex
  notes: |
    Start with `ota doctor`, then `ota validate`, then run `ota run dev` or `ota run dev:debug`.
    Keep bound task shapes deterministic; shared local backends are explicit identity, not a way to ignore conflicting container shape.
