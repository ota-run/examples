# Copyright (C) 2026 — 2026, Ota. All Rights Reserved.
# Licensed under the Apache License, Version 2.0. See LICENSE for the full license text.

version: 1
project:
  name: task-prepare-package-hydration
  description: Canonical first-class package hydration through a node package manager
  type: service

toolchains:
  node:
    version: "24.15.0"
    package_managers:
      pnpm: "10.33.4"
    fulfillment:
      source: corepack
      mode: run

tasks:
  install:
    description: Hydrate workspace dependencies through the declared pnpm install lane
    category: setup
    prepare:
      kind: dependency_hydration
      medium: package_dependencies
      source:
        kind: node_package_manager
        cwd: .
        manager: pnpm
        mode: install
        frozen_lockfile: true
    requirements:
      toolchains:
        - node
    effects:
      writes:
        - node_modules
      network: true
      network_kind: dependency_hydration

  build:
    description: Build after dependency hydration
    category: build
    run: pnpm build
    depends_on:
      - install
    requirements:
      toolchains:
        - node
