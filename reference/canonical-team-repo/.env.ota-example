# Canonical advanced repo example env source for ota `env.sources`.
#
# This file is intentionally non-secret so the example can dogfood declared dotenv
# sources, `must_exist`, and ordered env resolution without requiring local setup.

DOCS_SITE_BASE_URL=https://docs.example.internal
RELEASE_CHANNEL=stable
